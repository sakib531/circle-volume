#include<stdio.h>

int main (){
    float a;
    printf ("enter the value of 'a' which is the radius of a sphere:");
    scanf("%f",&a);
    printf ("the volume of the sphere's circle is :");
    printf ("%f" ,1.33*3.1426*a*a*a);
    return 0;
}
